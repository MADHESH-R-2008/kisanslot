# Local development only. Render supplies these values from its environment.
# Use a managed cloud database URL in production (for example MySQL via pymysql).
DATABASE_URL=sqlite:///./kisanslot.db
SECRET_KEY=replace-with-a-long-random-secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
AVERAGE_PROCESSING_TIME_MINUTES=6
